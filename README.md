# CareerOS

## Backend development

After adding or changing Maven runtime dependencies, fully stop and restart the backend process. Spring Boot DevTools hot restart may not refresh the JVM dependency classpath.
